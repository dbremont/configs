#!/usr/bin/env bash

set -euo pipefail

HOOK_DIR="$(dirname "$0")/pre-commit.d"

for hook in "$HOOK_DIR"/*.sh; do
    echo "==> $(basename "$hook")"
    bash "$hook"
done

echo
echo "✓ Pre-commit checks passed."